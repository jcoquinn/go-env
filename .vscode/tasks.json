{
  // See https://go.microsoft.com/fwlink/?LinkId=733558
  // for the documentation about the tasks.json format
  "version": "2.0.0",
  "options": {
    "cwd": "${workspaceFolder}"
  },
  "tasks": [
    {
      "label": "test: unit",
      "type": "shell",
      "command": "go",
      "args": ["test", "-v", "-skip", "Integration", "./..."],
      "group": "test"
    }
  ]
}
